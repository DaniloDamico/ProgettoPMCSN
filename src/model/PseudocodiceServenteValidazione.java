package model;

public class PseudocodiceServenteValidazione {

    /*
    while(true){
        scheduleResult;
        if (!CodaVIP.isEmpty()){
            scheduleResult = schedule first vip in queue
        } else if(!Coda.isEmpty()){
            scheduleResult = schedule = schedule first client in queue
        } else{
            goTo next iteration;
        }

        if(scheduleResult is successful){
            move job forward
        } else{
            send job back to Biglietteria
        }
    }

     */

}
